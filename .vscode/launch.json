// For VS Code users, helps with debugging.
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Main File",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "console": "integratedTerminal"
        }
    ]
}